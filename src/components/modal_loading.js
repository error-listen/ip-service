import React from 'react';

import '../styles/modal_loading.css';

function MODAL_LOADING() {
    return (
        <div className="modal_loading">
            <p>
                Loading...
            </p>
        </div>
    );
}

export default MODAL_LOADING;